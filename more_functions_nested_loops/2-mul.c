#include "main.h"
/**
 * mul - multiplies two integers and returns the result.
 * @n1: value to be multiplied.
 * @n2: value to be multiplied.
 * Return: the multiplication.
 */
int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
